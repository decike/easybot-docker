# .env.template - 填写后改名为 .env 放在仓库根目录

# 必填：镜像的完整名称（可以包含 registry，或仅为 org/name）
# 示例： my-org/my-image 或 my-registry.io/myorg/my-image
IMAGE_NAME=decike/easybot-docker

# 必填：镜像版本/标签
IMAGE_TAG=0.1.0

# 架构/平台（buildx 支持的格式）。默认 linux/amd64
# 例如： linux/amd64 或 linux/arm64
IMAGE_ARCH=linux/amd64

# 镜像作者/维护者（用于 Docker label，作为回退来源）
# 示例：Your Name <you@example.com> 或 my-org
AUTHOR=decike

# 可选：用于生成输出文件名中的“作者”字段（优先使用）
# 如果不填写，workflow 将尝试从 IMAGE_NAME 的组织/命名空间部分提取作者（IMAGE_NAME 的第一个 '/' 之前的部分）。
# 例如：IMAGE_NAME=my-org/my-image -> 抽取到 my-org 作为作者；若 IMAGE_NAME 没有 '/', 则回退到 AUTHOR 的提取逻辑。
# 例如：FILENAME_AUTHOR=my-org
FILENAME_AUTHOR=

# 可选：额外 build-args（以空格分隔的 key=value 列表）
# 例如：EXTRA_BUILD_ARGS="HTTP_PROXY=http://... ARG2=val"
EXTRA_BUILD_ARGS=

# 可选：Dockerfile 路径（相对于仓库根）
DOCKERFILE_PATH=Dockerfile

# 可选：构建上下文（默认 .）
# 请确保 BUILD_CONTEXT 包含 data/，否则 COPY data/ 会失败
BUILD_CONTEXT=.

# Release 配置
RELEASE_DRAFT=false
RELEASE_PRERELEASE=false
RELEASE_TAG_PREFIX=v

# 生成的 tar 文件名（可留空，默认会按 FILENAME_AUTHOR_IMAGEBASENAME_IMAGE_TAG-PLATFORM.tar.gz 生成）
# 如果你填写此项，则会直接使用该值（注意不要与其他并发运行冲突）
OUTPUT_TAR=

# 注意：请保持 .env 为简单的 KEY=VALUE 形式（无复杂引号或多行），actions 中会使用 shell source 加载。
